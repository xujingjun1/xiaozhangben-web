services:
  - type: web
    name: xiaozhangben
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: node server.js
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: 8080
