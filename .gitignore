# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# 环境变量文件（包含密钥）
.env
.env.local
api/.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vite

# Old build artifacts
deploy

# ============ 运行时数据（含用户隐私，禁止入库）============
/data/
api/data/db.json
api/data/db.json.bak*
api/data/backups/
*.genie

# ============ 发布包与测试产物 ============
deploy-pkg/
qa-screenshots/
.workbuddy/
.git-rewrite/
.last-git-backup.txt
# 本地推送辅助脚本（依赖本机代理端口与系统 Git，不入库）
push-to-github.bat

# 发布用 APK：唯一来源是 android 构建产物，由 scripts/prepare-deploy.mjs 同步。
# 曾入库一份 22MB 的手工副本，被 Vite 拷进 dist 后导致旧包上线（2026-09-25 修）。
public/app-latest.apk
