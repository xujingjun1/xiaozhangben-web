services:
  - type: web
    name: xiaozhangben-api
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node api/index.js
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
